type Person {
    id: ID!
    name: String!
    friends: [Person!]!
}

type Query {
    allPeople: [Person!]!
    findById(id: ID!): Person!
}

type Mutation {
    addFriend(firstFriendId: ID!, secondFriendId: ID!): Boolean!
}
